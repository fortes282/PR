/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // PWA: Next.js 14+ can use built-in manifest; service worker can be added via public/sw.js or @ducanh2912/next-pwa
};

module.exports = nextConfig;
