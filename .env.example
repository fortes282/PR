# --- Frontend (Next.js) ---
# Povinné: aplikace běží pouze s backendem. Bez http se zobrazí „Backend neběží“.
NEXT_PUBLIC_API_MODE=http
# URL backendu (lokálně nebo produkce)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
# Proxy přes Next.js (doporučeno pro Railway – vyhnutí CORS)
# NEXT_PUBLIC_USE_API_PROXY=true

# --- Backend (apps/api) ---
# See docs/About.md for full deployment and production checklist.
PORT=3001
JWT_SECRET=dev-secret-change-in-production
# CORS: comma-separated origins for production (e.g. https://web-production-xxx.up.railway.app)
# CORS_ORIGIN=https://web-production-21de7.up.railway.app
# SQLite path. Production: use absolute path, back up regularly.
# DATABASE_PATH=/var/lib/pristav/pristav.db

# --- SMTP (e-mail odesílání / testovací e-mail) ---
# Bez SMTP se notifikační e-maily neodesílají; testovací e-mail v Admin → Nastavení vrátí chybu.
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=notifikace@example.cz
# SMTP_PASS=your-app-password

# --- SMS (SMSAPI.com – ověření telefonu při registraci) ---
# OAuth token z SMSAPI portálu (https://ssl.smsapi.com/react/oauth/manage). Bez něj nelze odesílat SMS kódy.
# SMSAPI_TOKEN=your-oauth-token

# --- Web Push (VAPID) ---
# Vygenerujte: npx web-push generate-vapid-keys
# Veřejný klíč můžete vyplnit i v Admin → Nastavení (Push notifikace).
# VAPID_PUBLIC_KEY=BN...
# VAPID_PRIVATE_KEY=...
